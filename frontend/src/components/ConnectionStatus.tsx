// ConnectionStatus component removed - health checks are handled by keep-alive service
export function ConnectionStatus() {
  // Component disabled - no frontend health checks needed
  return null;
}